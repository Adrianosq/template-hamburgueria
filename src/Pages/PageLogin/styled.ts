import styled from "styled-components";

export const StyledPageLogin = styled.section`
    display: flex;
    align-items: center;
    justify-content: space-evenly;

    margin-top: 70px;

    @media (max-width: 600px) {
        flex-direction: column;
        padding: 10px;
    }
`